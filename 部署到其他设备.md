# 译尘 · Yichen 翻译工具 —— 在其他设备上部署运行

本文档用于把已打包好的项目迁移到**新设备**（另一台电脑/服务器）上运行。前端构建产物已包含在 `server/public/` 中，迁移时无需再次构建。

> 注意：源设备的 `server/.env`（含真实 Key）不会被随项目一起携带，迁移后需在目标设备重新配置（见下文）。

---

## 一、环境要求

- **Node.js 16+**（建议 18 LTS 或更高）
- 已联网（翻译请求会调用 https://uapis.cn 接口）
- 一个 Uapi API Key（免费注册：https://uapis.cn）

查看 Node 版本：

```bash
node -v
```

---

## 二、安装依赖

在**项目根目录**执行：

```bash
npm install
npm run install:all
```

- `npm install`：安装根目录的 `concurrently`（用于本地开发）
- `npm run install:all`：分别安装 `server/` 与 `web/` 的依赖

---

## 三、配置 API Key

两种方式任选其一。

**方式 A（推荐，纯文件）：**
```bash
copy server\.env.example server\.env      # Windows
# cp server/.env.example server/.env     # macOS / Linux
```
编辑 `server/.env`，填入真实 Key：
```
UAPI_API_KEY=你的真实Key
PORT=3000
WEB_ORIGIN=http://localhost:5173
```

**方式 B（网页配置）：**
先启动服务（见下一步），打开页面后点击右上角「配置 API Key」，粘贴 Key 即可。Key 会自动写入 `server/.env` 并**热更新立即生效，无需重启**。

> `WEB_ORIGIN` 仅用于开发模式 CORS 白名单，生产部署（前后端同源）可不改。

---

## 四、启动服务

在项目根目录执行：

```bash
npm start
```

启动成功后终端会显示：
```
✓ 翻译后端已启动: http://localhost:3000
  API Key: 已配置 / 未配置（请在网页配置或编辑 .env）
```

浏览器访问 **http://localhost:3000** 即可使用。

> 若需改端口，编辑 `server/.env` 的 `PORT` 后重启即可。
>
> 启动台页面：访问 `http://localhost:3000/launcher.html` 可看到带状态灯的启动台（绿点=服务运行中），一键进入翻译主站。

### 停止服务

- `npm start` 前台运行时：在终端按 `Ctrl + C` 即可停止
- Windows 下若服务是后台运行的，可双击项目根目录的 **`停止.bat`**（关闭占用 3000 端口的 node 进程），或在任务管理器结束 `node.exe`

---

## 五、部署到 Linux

项目为纯 Node.js 技术栈（Express + node-fetch），无 Windows 专属依赖，可平滑部署到 Linux 服务器。

### 1. 安装 Node.js（Ubuntu / Debian 示例）

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
node -v   # 确认 18+
```

### 2. 拷贝项目

```bash
# 从源设备拷贝整个项目（不含 node_modules），例如：
scp -r 02-translate user@你的服务器IP:~/ 
```

### 3. 安装依赖

```bash
cd ~/02-translate
npm install
npm run install:all
```

### 4. 配置 API Key

```bash
cp server/.env.example server/.env
vim server/.env    # 填入 UAPI_API_KEY
```

### 5. 启动与访问

```bash
npm start
```

访问 **http://你的服务器IP:3000** 即可。记得在服务器防火墙 / 云安全组中放行 `3000` 端口。

### 6. 开机自启（可选，systemd 常驻）

创建服务文件：

```bash
sudo nano /etc/systemd/system/translate.service
```

```ini
[Unit]
Description=Yichen Translate
After=network.target

[Service]
WorkingDirectory=/home/你的用户名/02-translate/server
ExecStart=/usr/bin/node index.js
Restart=always
Environment=PORT=3000

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable --now translate      # 开机自启并立即启动
sudo systemctl status translate            # 查看状态
sudo systemctl restart translate           # 重启
sudo systemctl stop translate              # 停止
```

### Linux 与 Windows 部署差异

| 项目 | Windows | Linux |
|------|---------|-------|
| `.env` 模板复制 | `copy server\.env.example server\.env` | `cp server/.env.example server/.env` |
| 停止服务 | `停止.bat` / 任务管理器 | `Ctrl+C` 或 `systemctl stop translate` |
| 后台运行 | bat 拉起 | `nohup` / `systemd` |
| 启动入口 | `npm start` | `npm start`（同左） |

> `停止.bat` 为 Windows 脚本，Linux 下无需使用。构建产物已含在 `server/public/`，迁移 Linux 无需重新 `npm run build`（除非修改前端后需重新打包）。

---

_本部署包基于 Vue 3 + Vite 前端、Express 后端，前后端由同一服务托管，零额外运行时依赖。_
